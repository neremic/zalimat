"use strict";

import React from 'react';

export const DisplayAppAndVersion = (props) =>
    <div>
        <span>
            My applicationId is: {this.props.applicationId}
        </span>
        <span>
            My versionId is: {this.props.versionId}
        </span>
    </div>;
